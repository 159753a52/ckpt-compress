# 快速调试指南

## 问题

所有稀疏度下准确率都是 0.9375，即使剪枝了 90% 的参数。

## 已添加的调试功能

### 1. 梯度检查
- 检测哪些参数没有梯度
- 检测哪些参数的重要性得分全为 0

### 2. 剪枝操作跟踪
- 显示剪枝前后的零值数量
- 显示实际新增的零值数量
- 验证参数是否真的被修改

### 3. 权重恢复验证
- 确认权重是否正确恢复到原始状态

## 运行命令

```bash
python experiments/scripts/bert_fine_grained_pruning_analysis.py \
    --dataset sst2 \
    --num_steps 10 \
    --batch_size 16 \
    --num_eval_batches 10 \
    --device cuda
```

## 关键调试输出位置

1. **梯度累积后**：查看是否有参数没有梯度或重要性得分为 0
2. **Embedding 层第一次剪枝**：查看详细的剪枝操作
3. **Block 0 第一次剪枝**：查看 Attention Query 的剪枝操作

## 预期发现

### 如果剪枝正常工作

```
=== 调试：剪枝操作 ===
  bert.encoder.layer.0.attention.self.query.weight:
    剪枝前: 0 个零, 1,048,576 个非零
    剪枝后: 52,429 个零, 996,147 个非零  ← 应该有新增的零
    新增零: 52,429 (5.00%)  ← 应该等于目标稀疏度
```

然后准确率应该随稀疏度增加而下降。

### 如果剪枝没有生效

```
=== 调试：剪枝操作 ===
  bert.encoder.layer.0.attention.self.query.weight:
    剪枝前: 0 个零, 1,048,576 个非零
    剪枝后: 0 个零, 1,048,576 个非零  ← 没有新增零！
    新增零: 0 (0.00%)  ← 问题在这里
```

这说明：
- 重要性得分可能全为 0
- 或者所有得分都相同
- 或者 mask 计算有问题

## 可能的根本原因

### 原因 1：重要性得分全为 0
**检查**：查看"重要性得分统计"输出
**解决**：增加 `--num_steps` 到 50-100

### 原因 2：评估批次太少导致统计噪声
**检查**：160 个样本可能不够
**解决**：增加 `--num_eval_batches` 到 50-100

### 原因 3：模型状态问题
**检查**：查看"模型模式"输出
**解决**：已在 `evaluate_metric` 中添加 `model.train()` 恢复

## 下一步

1. 运行脚本并查看调试输出
2. 检查"新增零"数量是否符合预期
3. 如果新增零为 0，检查重要性得分分布
4. 如果新增零正常但准确率不变，增加评估批次数
