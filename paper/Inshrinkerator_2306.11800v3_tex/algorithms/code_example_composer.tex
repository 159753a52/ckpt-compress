\begin{lstlisting}[style=PyStyle,caption={\small 
 MosaicML Composer application with \sys callback integration.},label={lst:code-composer}]
from composer import Trainer
from inshrinkerator.integrations.composer import InshrinkeratorCallback
# Add InshrinkeratorCallback to list of callbacks
trainer = Trainer(
    ...
    callbacks=[InshrinkeratorCallback()],
)
trainer.fit()
\end{lstlisting}
